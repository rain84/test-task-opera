import { Canvas, Story, Meta, ArgsTable } from '@storybook/addon-docs'
import * as Icon from 'ui/icon'
import { ButtonRoom } from './index.ts'

<Meta
  title="Buttons/ButtonRoom"
  component={ButtonRoom}
  argTypes={{
    color: {
      name: 'color',
      control: 'select',
      options: ['cyan', 'blue', 'orange', 'magenta'],
      defaultValue: 'cyan',
    },
  }}
/>

# ButtonRoom

export const Template = (args) => (
  <div className="flex p-2 space-x-4">
    <ButtonRoom {...args} color="cyan">
      <Icon.IconSofa className="fill-white" />
    </ButtonRoom>
    <ButtonRoom {...args} color="blue">
      <Icon.IconPot className="fill-white" />
    </ButtonRoom>
    <ButtonRoom {...args} color="orange">
      <Icon.IconBulb className="fill-white" />
    </ButtonRoom>
    <ButtonRoom {...args} color="magenta">
      <Icon.IconBathtub className="fill-white" />
    </ButtonRoom>
    <ButtonRoom {...args}>
      <Icon.IconCog className="fill-white" />
    </ButtonRoom>
  </div>
)

<Canvas>
  <Story
    name="Default"
    args={{
      pressed: false,
      disabled: false,
      className: '',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
