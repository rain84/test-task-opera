import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import { decorators } from 'config/storybook'
import { IconSnowflake as Icon } from '../'

<Meta
  title="UI/Icons"
  component={Icon}
  decorators={decorators}
  argTypes={{
    className: {
      control: 'text',
    },
  }}
/>

# UI / Icons / IconSnowflake

export const Template = ({ className }) => (
  <>
    <Icon className={className} />
    <Icon className="icon-medium cursor-pointer hover:bg-selected" />
    <Icon className="icon-white bg-red" />
  </>
)

<Canvas withToolbar>
  <Story name="Snowflake">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Snowflake" />
