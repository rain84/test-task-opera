import { Canvas, Story, Meta, ArgsTable } from '@storybook/addon-docs'

import { decorators, className } from 'config/storybook.tsx'
import { Modes } from './modes.component'

<Meta title="Layout" component={Modes} decorators={decorators} />

# Layout / Modes

export const Template = (args) => <Modes {...args} />

<Canvas withToolbar>
  <Story name="Modes" args={{ className }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Modes" />
