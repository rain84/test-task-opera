import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import { decorators } from 'config/storybook'
import { IconLeaf as Icon } from '../'

<Meta
  title="Icons"
  component={Icon}
  decorators={decorators}
  argTypes={{
    className: {
      control: 'text',
    },
  }}
/>

# IconLeaf

export const Template = ({ className }) => (
  <>
    <Icon className={className} />
    <Icon className="icon-medium cursor-pointer hover:bg-selected" />
    <Icon className="icon-white bg-red" />
  </>
)

<Canvas withToolbar>
  <Story name="Leaf">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Leaf" />
