import { Canvas, Story, Meta, ArgsTable } from '@storybook/addon-docs'
import * as Icon from 'ui/icon'
import { ButtonRoom } from './index.ts'

<Meta
  title="Buttons/ButtonRoom"
  component={ButtonRoom}
	decorators={[(story) => <div className="flex p-2 space-x-4">{story()}</div>]}
/>

# ButtonMode

export const Template = (args) => (
  <>
		<ButtonRoom color='cyan' {...args}>
			<Icon.IconSofa className="fill-white" />
		</ButtonRoom>
		<ButtonRoom color='blue' {...args}>
			<Icon.IconPot className="fill-white" />
		</ButtonRoom>
		<ButtonRoom color='orange' {...args}>
			<Icon.IconBulb	 className="fill-white" />
		</ButtonRoom>
		<ButtonRoom color='magenta' {...args}>
			<Icon.IconBathtub className="fill-white" />
		</ButtonRoom>
  </>
)

<Canvas>
	<Story name="Default" args={{ disabled: false }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story name="Disabled" args={{ disabled: true }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story name="Colors" args={{ disabled: true, color: 'magenta' }}>
		{Template.bind({})}
	</Story>
</Canvas>

<ArgsTable of={ButtonRoom} />
