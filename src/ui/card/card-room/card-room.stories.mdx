import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import { IconSofa, ButtonRoom } from 'ui'
import { CardRoom } from '.'
import { decorators, className } from 'config/storybook.tsx'

<Meta
  title="UI/Card"
  component={CardRoom}
  decorators={decorators}
  argTypes={{
    icon: { table: { disable: true } },
    onClick: { table: { disable: true } },
  }}
/>

# UI / Card / CardRoom

export const Template = (args) => (
  <CardRoom
    {...args}
    icon={<IconSofa className="fill-white" />}
    className={args.className}
    onClick={(e, { pressed }) =>
      alert(`ButtonRoom ${pressed ? 'pressed' : 'released'}`)
    }
  />
)

<Canvas withToolbar>
  <Story
    name="CardRoom"
    args={{
      color: 'magenta',
      title: '19 °C',
      label: 'Living room',
      pressed: false,
      className: `${className} p-5`,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="CardRoom" />
