import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import { decorators } from 'config/storybook'
import { IconSettings as Icon } from '../'

<Meta
  title="UI/Icons"
  decorators={decorators}
  component={Icon}
  argTypes={{
    className: {
      control: 'text',
    },
  }}
/>

# UI / Icons / IconSettings

export const Template = ({ className }) => (
  <>
    <Icon className={className} />
    <Icon className="icon-medium cursor-pointer hover:bg-selected" />
    <Icon className="icon-white bg-red" />
  </>
)

<Canvas withToolbar>
  <Story name="Settings">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Settings" />
