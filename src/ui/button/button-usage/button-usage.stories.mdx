import { Canvas, Story, Meta, ArgsTable } from '@storybook/addon-docs'

import { decorators } from 'config/storybook.tsx'
import * as Icon from 'ui/icon'
import { ButtonUsage } from './index.ts'

<Meta title="UI/Buttons" component={ButtonUsage} decorators={decorators} />

# UI / Buttons / ButtonUsage

export const Template = (args) => (
  <>
    <ButtonUsage className="group" {...args}>
      <Icon.IconThermometer className="group-hover:fill-primary" />
    </ButtonUsage>
    <ButtonUsage className="group" {...args}>
      <Icon.IconDrop className="group-hover:fill-primary" />
    </ButtonUsage>
  </>
)

<Canvas withToolbar>
  <Story
    name="ButtonUsage"
    args={{
      pressed: false,
      disabled: false,
      className: '',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="ButtonUsage" />
