import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import { IconSnowflake, ButtonMode } from 'ui'
import { Card } from '.'
import { decorators } from 'config/storybook.tsx'

<Meta title="Card" component={Card} decorators={decorators} />

# Card for 'Mode'

export const Template = (args) => (
  <Card {...args}>
    <ButtonMode
      onClick={(e, { pressed }) =>
        alert(`ButtonRoom ${pressed ? 'pressed' : 'released'}`)
      }
      className="mb-2 group"
    >
      <IconSnowflake className="group-hover:fill-primary" />
    </ButtonMode>
  </Card>
)

<Canvas withToolbar>
  <Story
    name="Card for \'Mode\'"
    args={{
      title: 'Cool air',
      label: 'On',
      className: 'border p-5 rounded-lg',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Card for \'Mode\'" />
