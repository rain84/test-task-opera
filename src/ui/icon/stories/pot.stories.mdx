import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'
import { IconPot as Icon } from '../'

<Meta
  title="Icons"
  component={Icon}
  argTypes={{
    className: {
      control: 'text',
    },
  }}
/>

# IconPot

export const Template = ({ className }) => (
  <div className="flex p-2 space-x-4">
    <Icon className={className} />
    <Icon className="icon-medium cursor-pointer hover:bg-selected" />
    <Icon className="icon-white bg-red" />
  </div>
)

<Canvas>
  <Story name="Pot">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Pot" />
