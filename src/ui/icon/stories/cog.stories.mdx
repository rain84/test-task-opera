import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import { decorators } from 'config/storybook'
import { IconCog as Icon } from '../'

<Meta
  title="Icons"
  component={Icon}
  decorators={decorators}
  argTypes={{
    className: {
      control: 'text',
    },
  }}
/>

# IconCog

export const Template = ({ className }) => (
  <>
    <Icon className={className} />
    <Icon className="icon-medium cursor-pointer hover:bg-selected" />
    <Icon className="icon-white bg-red" />
  </>
)

<Canvas withToolbar>
  <Story name="Cog">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Cog" />
