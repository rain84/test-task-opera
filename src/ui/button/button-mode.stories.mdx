import { Canvas, Story, Meta, ArgsTable } from '@storybook/addon-docs'
import * as Icon from 'ui/icon'
import { ButtonMode } from './index.ts'

<Meta
  title="Buttons/ButtonMode"
  component={ButtonMode}
	decorators={[(story) => <div className="flex p-2 space-x-4">{story()}</div>]}
/>

# ButtonMode

export const Template = (args) => (
	<>
		<ButtonMode {...args} className="group">
			<Icon.IconSnowflake className="group-hover:fill-primary" />
		</ButtonMode>
		<ButtonMode {...args} className="group">
			<Icon.IconLeaf className="group-hover:fill-primary" />
		</ButtonMode>
		<ButtonMode {...args} className="group">
			<Icon.IconBellOff className="group-hover:fill-primary" />
		</ButtonMode>
	</>
)

<Canvas>
	<Story name="Default" args={{ disabled: false }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Disabled"
		args={{ disabled: true }}
	>
		{Template.bind({})}
	</Story>
</Canvas>

<ArgsTable of={ButtonMode} />
