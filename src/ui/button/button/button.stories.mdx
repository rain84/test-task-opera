import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import { decorators, className } from 'config/storybook.tsx'
import * as Icon from 'ui/icon'
import { Button } from './index.ts'

<Meta title="UI/Buttons" component={Button} decorators={decorators} />

# UI / Buttons / Bare `<Button/>` without any styles, borders, etc.

export const Template = (args) => <Button {...args}>Default Button</Button>

<Canvas withToolbar>
  <Story
    name="Base button"
    args={{
      pressed: false,
      disabled: false,
      className: `${className} p-2`,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Base button" />
