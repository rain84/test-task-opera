import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import { decorators } from 'config/storybook'
import { IconDrop as Icon } from '../'

<Meta
  title="UI/Icons"
  component={Icon}
  decorators={decorators}
  argTypes={{
    className: {
      control: 'text',
    },
  }}
/>

# UI / Icons / IconDrop

export const Template = ({ className }) => (
  <>
    <Icon className={className} />
    <Icon className="icon-medium cursor-pointer hover:bg-selected" />
    <Icon className="icon-white bg-red" />
  </>
)

<Canvas withToolbar>
  <Story name="Drop">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Drop" />
