import { Canvas, Story, Meta, ArgsTable } from '@storybook/addon-docs'

import { decorators, className } from 'config/storybook.tsx'
import { Header } from '.'

<Meta title="Layout" component={Header} decorators={decorators} />

# Layout / Header

export const Template = (args) => <Header {...args} />

<Canvas withToolbar>
  <Story name="Header" args={{ className }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Header" />
