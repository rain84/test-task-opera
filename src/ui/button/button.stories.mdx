import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import { decorators } from 'config/storybook.tsx'
import * as Icon from 'ui/icon'
import { Button } from './index.ts'

<Meta title="Buttons" component={Button} decorators={decorators} />

## Bare `<Button/>` without any styles, borders, etc.

export const Template = (args) => <Button {...args}>Default Button</Button>

<Canvas withToolbar>
  <Story
    name="Base button"
    args={{
      pressed: false,
      disabled: false,
      className: 'p-2 border rounded-2xl',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Base button" />
