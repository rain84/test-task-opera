import { Canvas, Meta, Story } from '@storybook/addon-docs'
import * as Icons from '.'
// prettier-ignore
const names = [
	'Menu', 'Bathtub', 'BellOff', 'Bulb', 'Cog', 'Drop', 'Leaf', 'Podium', 'Pot', 'Snowflake', 'Sofa', 'Thermometer', 'Torchere', 'Settings',
]
const icon = (name) => {
  const Component = Icons[`Icon${name}`]
  return (
    <div className="p-2 flex space-x-4">
      <Component />
      <Component className="icon-disabled" />
      <Component className="icon-white bg-red" />
      <Component className="icon-2x cursor-pointer hover:bg-selected" />
    </div>
  )
}

<Meta title="Icons" />

# Icons

<Canvas>
  <Story name="All-in-One">{names.map(icon)}</Story>
  <Story name="Menu">{icon('Menu')}</Story>
  <Story name="Bathtub">{icon('Bathtub')}</Story>
  <Story name="BellOff">{icon('BellOff')}</Story>
  <Story name="Bulb">{icon('Bulb')}</Story>
  <Story name="Cog">{icon('Cog')}</Story>
  <Story name="Drop">{icon('Drop')}</Story>
  <Story name="Leaf">{icon('Leaf')}</Story>
  <Story name="Podium">{icon('Podium')}</Story>
  <Story name="Pot">{icon('Pot')}</Story>
  <Story name="Snowflake">{icon('Snowflake')}</Story>
  <Story name="Sofa">{icon('Sofa')}</Story>
  <Story name="Thermometer">{icon('Thermometer')}</Story>
  <Story name="Torchere">{icon('Torchere')}</Story>
  <Story name="Settings">{icon('Settings')}</Story>
</Canvas>
