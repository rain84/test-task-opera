import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import { decorators } from 'config/storybook.tsx'
import { User } from '.'

<Meta title="User" decorators={decorators} />

# User

export const Template = (args) => <User {...args} />

<Canvas withToolbar>
  <Story
    name="User"
    args={{
      focus: false,
      className: '',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="User" />
