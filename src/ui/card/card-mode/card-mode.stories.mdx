import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import { IconSnowflake } from 'ui'
import { CardMode } from '.'
import { decorators } from 'config/storybook.tsx'

<Meta
  title="UI/Card"
  component={CardMode}
  decorators={decorators}
  argTypes={{
    icon: { table: { disable: true } },
    label: { table: { disable: true } },
    onClick: { table: { disable: true } },
  }}
/>

# UI / Card / CardMode

export const Template = (args) => (
  <CardMode
    {...args}
    key={args.title}
    icon={IconSnowflake}
    className={`mr-[34px] last:mr-0 ${args.className}`}
  />
)

<Canvas withToolbar>
  <Story
    name="CardMode"
    args={{
      title: 'Cool air',
      pressed: false,
      onClick: (_, state) => alert(JSON.stringify(state, null, ' ')),
      className: 'border p-5 rounded-lg',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="CardMode" />
