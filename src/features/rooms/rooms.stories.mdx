import { useState, useRef, useDeferredValue } from 'react'
import { Canvas, Story, Meta, ArgsTable } from '@storybook/addon-docs'

import { Rooms } from './rooms.component'

<Meta title="Layout/Rooms" component={Rooms} />

# Toggle

export const Template = (args) => {
  return (
    <div className="w-[360px] elevation-big">
      <Rooms />
    </div>
  )
}

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Default" />
