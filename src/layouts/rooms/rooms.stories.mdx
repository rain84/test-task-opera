import { Canvas, Story, Meta, ArgsTable } from '@storybook/addon-docs'

import { decorators, className } from 'config/storybook.tsx'
import { Rooms } from './rooms.component'

<Meta title="Layout" component={Rooms} decorators={decorators} />

# Layout / Rooms

export const Template = (args) => (
  <Rooms className={`w-[360px] ${args.className}`} />
)

<Canvas withToolbar>
  <Story name="Rooms" args={{ className }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Rooms" />
