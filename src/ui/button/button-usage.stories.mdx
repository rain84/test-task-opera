import { Canvas, Story, Meta, ArgsTable } from '@storybook/addon-docs'
import * as Icon from 'ui/icon'
import { ButtonUsage } from './index.ts'

<Meta
  title="Buttons/ButtonUsage"
  component={ButtonUsage}
	decorators={[(story) => <div className="flex p-2 space-x-4">{story()}</div>]}
/>

# ButtonUsage

export const Template = (args) => (
	<>
		<ButtonUsage {...args} className="group">
			<Icon.IconThermometer className="group-hover:fill-primary"/>
		</ButtonUsage>
		<ButtonUsage {...args} className="group">
			<Icon.IconDrop className="group-hover:fill-primary"/>
		</ButtonUsage>
	</>
)

<Canvas>
	<Story name="Default" args={{ disabled: false }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Disabled"
		args={{ disabled: true }}
	>
		{Template.bind({})}
	</Story>
</Canvas>

<ArgsTable of={ButtonUsage} />
