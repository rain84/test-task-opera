import { Canvas, Story, Meta, ArgsTable } from '@storybook/addon-docs'

import { decorators } from 'config/storybook.tsx'
import { Rooms } from './rooms.component'

<Meta title="Layout" component={Rooms} decorators={decorators} />

# Layout / Rooms

export const Template = (args) => <Rooms className="w-[360px] elevation-big" />

<Canvas withToolbar>
  <Story name="Rooms">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Rooms" />
