import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import { IconSofa, ButtonRoom } from 'ui'
import { Card } from '.'
import { decorators } from 'config/storybook.tsx'

<Meta title="Card" component={Card} decorators={decorators} />

# Card for 'Room'

export const Template = (args) => (
  <Card {...args}>
    <ButtonRoom
      color="magenta"
      onClick={(e, { pressed }) =>
        alert(`ButtonRoom ${pressed ? 'pressed' : 'released'}`)
      }
      className="mb-2"
    >
      <IconSofa className="fill-white" />
    </ButtonRoom>
  </Card>
)

<Canvas withToolbar>
  <Story
    name="Card for \'Room\'"
    args={{
      title: '19°C',
      label: 'Living room',
      className: 'border p-5 rounded-lg',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Card for \'Room\'" />
