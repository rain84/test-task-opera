import { Canvas, Story, Meta, ArgsTable } from '@storybook/addon-docs'

import { decorators } from 'config/storybook.tsx'
import * as Icon from 'ui/icon'
import { ButtonMode } from './index.ts'

<Meta title="UI/Buttons" component={ButtonMode} decorators={decorators} />

# UI / Buttons / ButtonMode

export const Template = (args) => (
  <>
    <ButtonMode className="group" {...args}>
      <Icon.IconSnowflake className="group-hover:fill-primary" />
    </ButtonMode>
    <ButtonMode className="group" {...args}>
      <Icon.IconLeaf className="group-hover:fill-primary" />
    </ButtonMode>
    <ButtonMode className="group" {...args}>
      <Icon.IconBellOff className="group-hover:fill-primary" />
    </ButtonMode>
  </>
)

<Canvas withToolbar>
  <Story
    name="ButtonMode"
    args={{
      pressed: false,
      disabled: false,
      className: '',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="ButtonMode" />
